from libdebug import debugger

d=debugger("test/binaries/trace_test")
d.run()
bp = d.breakpoint(0x40119e, hardware=True)
bbp= d.breakpoint(0x401167, hardware=True)
bbbp = d.breakpoint(0x401184,hardware=True)
print(hex(d.regs.rip))
d.cont()
d.trace()
d.cont()
d.trace()
d.cont()
d.trace()
print(hex(d.regs.rip))
d.cont()
print(hex(d.regs.rip))
d.kill()
